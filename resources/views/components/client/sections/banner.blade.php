<div class="page-hero bg-image overlay-dark" style="background-image: url({{ asset($banner) }});">
	<div class="hero-section">
		<div class="container text-center wow zoomIn">
			<span class="subhead">{{ $sub }}</span>
			<h1 class="display-4">{{ $title }}</h1>
			<a href="{{ $link }}" class="btn btn-primary">{{ $textButton }}</a>
		</div>
	</div>
</div>
